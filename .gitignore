# Reference repositories
mlgruby-repo-for-reference/
.claude/

# BATS test helpers (git clones)
tests/test_helper/bats-support/
tests/test_helper/bats-assert/
.DS_Store

# User-specific configuration (generated during bootstrap, not committed)
# Each machine generates its own user-config.nix with correct paths and settings
user-config.nix
user-config.nix.backup
